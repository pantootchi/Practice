const superHeroes: string[] = [];
const heroPower: number[] = [];
const heroPower2: Array<number> = [];

type User = {
    name: string
    isActive: boolean
}

const allUsers: User[] = [];

const MLModels: number[][] = [
    [255, 255, 255],
    []
]

superHeroes.push("spiderman");
heroPower.push(2);

allUsers.push({name: "Pat", isActive: true});

export{};